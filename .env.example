# Database path (absolute path for persistent storage on server)
DB_PATH=/home/ubuntu/srm-data/srm.db

# Server port
PORT=3000

# Environment
NODE_ENV=production

# JWT Secret (change this in production!)
JWT_SECRET=aden-srm-prototype-secret-key-2026
